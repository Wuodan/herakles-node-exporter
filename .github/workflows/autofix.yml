name: Rust Autofix

on:
  schedule:
    - cron: "19 4 * * 1"
  workflow_dispatch:
    inputs:
      mode:
        description: How to apply autofixes
        required: true
        default: pull_request
        type: choice
        options:
          - pull_request
          - commit
      fail-on-warnings:
        description: Fail if warnings remain after autofix
        required: false
        default: true
        type: boolean

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: rust-autofix-${{ github.ref }}
  cancel-in-progress: true

jobs:
  autofix:
    runs-on: ubuntu-latest
    env:
      AUTOFIX_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'pull_request' }}
      FAIL_ON_WARNINGS: ${{ github.event_name == 'workflow_dispatch' && inputs['fail-on-warnings'] || 'true' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Validate mode
        shell: bash
        run: |
          set -euo pipefail
          echo "AUTOFIX_MODE=${AUTOFIX_MODE}"
          echo "FAIL_ON_WARNINGS=${FAIL_ON_WARNINGS}"
          echo "github.ref_name=${{ github.ref_name }}"
          echo "default_branch=${{ github.event.repository.default_branch }}"
          if [[ "${AUTOFIX_MODE}" == "commit" && "${{ github.ref_name }}" == "${{ github.event.repository.default_branch }}" ]]; then
            echo "Refusing to auto-commit directly to the default branch."
            exit 1
          fi

      - name: Run Clippy autofix
        shell: bash
        run: |
          set -euo pipefail
          cargo clippy --fix --allow-dirty --allow-staged --all-targets --no-default-features
          cargo fmt --all
          if [[ "${FAIL_ON_WARNINGS}" == "true" ]]; then
            cargo clippy --all-targets --no-default-features -- -D warnings
          fi

      - name: Detect changes
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "has_changes=false" >> "${GITHUB_OUTPUT}"
            echo "has_changes=false"
          else
            echo "has_changes=true" >> "${GITHUB_OUTPUT}"
            echo "has_changes=true"
          fi

      - name: Commit autofixes to branch
        if: steps.changes.outputs.has_changes == 'true' && env.AUTOFIX_MODE == 'commit'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Apply Rust autofixes"
          git push origin "HEAD:${{ github.ref_name }}"

      - name: Create autofix PR
        if: steps.changes.outputs.has_changes == 'true' && env.AUTOFIX_MODE == 'pull_request'
        uses: peter-evans/create-pull-request@v7
        with:
          branch: rust-autofix/${{ github.ref_name }}
          delete-branch: true
          commit-message: "Apply Rust autofixes"
          title: "Apply Rust autofixes"
          body: |
            Automated fixes generated by:

            - cargo clippy --fix --all-targets --no-default-features
            - cargo fmt --all

      - name: Report no changes
        if: steps.changes.outputs.has_changes != 'true'
        run: echo "No autofixes were produced."
